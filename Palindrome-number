class Solution:
    def isPalindrome(self, x: int) -> bool:
        x = str(x)        
        for i in range(len(str(x))):            
            if x[i]!=x[-(i+1)]:
                return False
            else:
                continue
        return True
