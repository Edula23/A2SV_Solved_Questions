n, s =  map(int, input().split())
nums = list(map(int, input().split()))
count = 0
for left in range(n):
    summ = 0
    right = left
    while right < n:
        summ+=nums[right]
        if summ >= s:
            count+=1
        right += 1
print(count)
