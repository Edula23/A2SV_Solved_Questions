n, k, q = map(int, input().split())
freq = [0] * 200005
for i in range(n):
    l, r = map(int, input().split())
    freq[l] += 1
    freq[r+1] -= 1

for i in range(1, len(freq)):
    freq[i] += freq[i-1]

c = 0
for i in range(1, len(freq)):
    if freq[i] >= k:
        c+=1
    freq[i] = c
for i in range(q):
    a, b = map(int, input().split())
    print(freq[b] - freq[a-1])
    
