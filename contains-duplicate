class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        dic = {}
        for n in nums:
            if n not in dic:
                dic[n] = 0
            dic[n] += 1
        for value in dic.values():
            if value >= 2:
                return True
        return False
